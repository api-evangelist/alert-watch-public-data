# alert-watch-public-data
Read-only static JSON snapshots derived from GOV.UK Emergency Alerts pages and the GDACS API, exposed as unauthenticated HTTPS GET endpoints with an OpenAPI 3.1 contract and an llms.txt discovery file. Explicitly unofficial and informational; not an emergency service.
